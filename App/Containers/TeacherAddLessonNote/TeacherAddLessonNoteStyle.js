import {StyleSheet} from 'react-native';
import {Colors} from 'App/Theme';

export default StyleSheet.create({
  scrollview: {
    // flexGrow: 1,
    flex: 1,
    // backgroundColor: 'red',
  },
});
